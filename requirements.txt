anthropic>=0.40.0
feedparser>=6.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
rich>=13.0.0
websockets>=12.0
tweepy>=4.14.0
aiohttp>=3.9.0
# FIX: py-clob-client is now pinned with a minimum version so all users get a
# consistent API surface for executor.py (OrderArgs, OrderType.GTC, etc.).
# Required for live trading. Install with: pip install py-clob-client>=0.18.0
py-clob-client>=0.18.0
